#include <stdio.h>
#include <stdlib.h>
#include <cuda.h>
#include <curand.h>
#include <curand_kernel.h>
#include <time.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>
#include <cuda_runtime_api.h>

__global__ void conta(float*x,float*, int*conta,N){
		int id=threadIdx.x+blockIdx.x*blockDim.x;	
		for(i=0;i<N;i++){
			}
main(){
int i=0;
float a,b;
int nof=20;
FILE*f;f=fopen("./posizione/dati_0","r");
					while(fscanf(f,"%f" "%f",&a,&b)>0)i++;
					rewind(f);
float* x;float* y;
float* x1;float* y1;
x=(float*)malloc(N*nof*sizeof(float));y=(float*)malloc(N*nof*sizeof(float));
FILE*g;g=fopen("in","r");
					while(fscanf(f,"%f" "%f",&a,&b)>0)i++;
					rewind(f);


